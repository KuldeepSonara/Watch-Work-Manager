export enum ApiEndpoints {
    WORKERS = '/workers',
    TASKS_MANAGEMENT = '/manage-tasks',
    TASKS = '/tasks',
    ENTRIES = '/entries',
    PAYMENTS = '/worker-payments'
}

export enum WorkStatus {
    IN_PROGRESS = 'in_progress',
    COMPLETED = 'completed',
    PAID = 'paid'
}
